#include <stdio.h>
#include "main.h"
/**
 * main - writes program name
 * @argc: count
 * @argv: pointer to strings
 * Return: zero
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);

}
