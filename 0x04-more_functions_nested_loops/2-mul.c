#include "main.h"
/**
 * mul - writes the character c to stdout
 * @a: The integer to print
 * @b: The integer to print
 * Return: multiply
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
